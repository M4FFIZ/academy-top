meta:
  environment: node
  toolchain:
    name: npm
    version: "22"

build:
  additionalCommands: npm run db:generate && npm run build -w @academy-top/api

run:
  command: npm run amvera:api:start
  containerPort: 3001